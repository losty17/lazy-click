services:
  oauth-backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: lazy-click-oauth-backend
    env_file:
      - backend/.env
    ports:
      - "8787:8787"
    restart: unless-stopped
