CLICKUP_API_TOKEN=
CLICKUP_CLIENT_ID=
# backend-only secret (do not set this in the TUI env for production deployments)
CLICKUP_CLIENT_SECRET=
LAZY_CLICK_OAUTH_BACKEND_URL=http://127.0.0.1:8787
LAZY_CLICK_DEFAULT_PROVIDER=local
# optional override (default is ~/.local/share/lazy-click/lazy-click.db)
# COMMAND_TASK_DB_PATH=/absolute/path/to/lazy-click.db
